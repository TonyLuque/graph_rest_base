type User {
  id: ID!
  email: String!
  password: String!
  role: String!
  profile: Profile
  createdAt: String!
  updatedAt: String!
}

type Query {
  user: User
  users: [User]
}

type Mutation {
  oa: String
}
